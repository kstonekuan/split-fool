{
  "name": "split-fool",
  "version": "1.0.0",
  "description": "Open source expense tracking app",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter '*' dev",
    "build": "pnpm --filter '*' build",
    "typecheck": "pnpm --filter '*' typecheck",
    "lint": "pnpm --filter '*' lint",
    "format": "pnpm --filter '*' format"
  },
  "keywords": [
    "expense-tracking",
    "splitwise-alternative"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.12.2",
  "devDependencies": {
    "@biomejs/biome": "2.0.4",
    "wrangler": "^4.20.5"
  }
}
