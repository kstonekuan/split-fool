# Environment variables are automatically set by Docker Compose
# This file is for reference only when using Docker

# DynamoDB Configuration
NODE_ENV=development
DYNAMODB_ENDPOINT=http://dynamodb:8000  # Docker service name
TABLE_NAME=splitfool-dev
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local

# API Configuration
PORT=3000